# Deny direct web access to non-document files.
# App is served from repo root, so protect the source-only dirs.

<DirectoryMatch "^/var/www/html/(config|includes|sql|logs)(/.*)?$">
    Require all denied
</DirectoryMatch>

<FilesMatch "\.(sql|md|log)$">
    Require all denied
</FilesMatch>
